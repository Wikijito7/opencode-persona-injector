# Jungle Mode

OpenCode plugin that injects playful jungle personas into agent system prompts.

## Structure

- `jungle-mode.tsx` — TUI plugin entry point
- `jungle-mode-server.ts` — Server plugin entry point
- `jungle-mode/` — Sub-modules (persona, command, prompt-indicator, types)
- `tests/` — Unit tests
